block variables

doctype html
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title #{title}
    body
        header
            img(class="logo-48 colors-types-wrapper__logo" src="../logo.svg" alt="logo")
        block content